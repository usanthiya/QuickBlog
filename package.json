{
  "name": "quickblog-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build:client": "npm run build --workspace=client"
  },
  "devDependencies": {
    "netlify-cli": "^16.0.0"
  }
}
